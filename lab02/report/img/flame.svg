<?xml version="1.0" standalone="no"?>
<!DOCTYPE svg PUBLIC "-//W3C//DTD SVG 1.1//EN" "http://www.w3.org/Graphics/SVG/1.1/DTD/svg11.dtd">
<svg version="1.1" width="1200" height="326" onload="init(evt)" viewBox="0 0 1200 326" xmlns="http://www.w3.org/2000/svg" xmlns:xlink="http://www.w3.org/1999/xlink">
<!-- Flame graph stack visualization. See https://github.com/brendangregg/FlameGraph for latest version, and http://www.brendangregg.com/flamegraphs.html for examples. -->
<!-- NOTES:  -->
<defs>
	<linearGradient id="background" y1="0" y2="1" x1="0" x2="0" >
		<stop stop-color="#eeeeee" offset="5%" />
		<stop stop-color="#eeeeb0" offset="95%" />
	</linearGradient>
</defs>
<style type="text/css">
	text { font-family:Verdana; font-size:12px; fill:rgb(0,0,0); }
	#search, #ignorecase { opacity:0.1; cursor:pointer; }
	#search:hover, #search.show, #ignorecase:hover, #ignorecase.show { opacity:1; }
	#subtitle { text-anchor:middle; font-color:rgb(160,160,160); }
	#title { text-anchor:middle; font-size:17px}
	#unzoom { cursor:pointer; }
	#frames > *:hover { stroke:black; stroke-width:0.5; cursor:pointer; }
	.hide { display:none; }
	.parent { opacity:0.5; }
</style>
<script type="text/ecmascript">
<![CDATA[
	"use strict";
	var details, searchbtn, unzoombtn, matchedtxt, svg, searching, currentSearchTerm, ignorecase, ignorecaseBtn;
	function init(evt) {
		details = document.getElementById("details").firstChild;
		searchbtn = document.getElementById("search");
		ignorecaseBtn = document.getElementById("ignorecase");
		unzoombtn = document.getElementById("unzoom");
		matchedtxt = document.getElementById("matched");
		svg = document.getElementsByTagName("svg")[0];
		searching = 0;
		currentSearchTerm = null;

		// use GET parameters to restore a flamegraphs state.
		var params = get_params();
		if (params.x && params.y)
			zoom(find_group(document.querySelector('[x="' + params.x + '"][y="' + params.y + '"]')));
                if (params.s) search(params.s);
	}

	// event listeners
	window.addEventListener("click", function(e) {
		var target = find_group(e.target);
		if (target) {
			if (target.nodeName == "a") {
				if (e.ctrlKey === false) return;
				e.preventDefault();
			}
			if (target.classList.contains("parent")) unzoom(true);
			zoom(target);
			if (!document.querySelector('.parent')) {
				// we have basically done a clearzoom so clear the url
				var params = get_params();
				if (params.x) delete params.x;
				if (params.y) delete params.y;
				history.replaceState(null, null, parse_params(params));
				unzoombtn.classList.add("hide");
				return;
			}

			// set parameters for zoom state
			var el = target.querySelector("rect");
			if (el && el.attributes && el.attributes.y && el.attributes._orig_x) {
				var params = get_params()
				params.x = el.attributes._orig_x.value;
				params.y = el.attributes.y.value;
				history.replaceState(null, null, parse_params(params));
			}
		}
		else if (e.target.id == "unzoom") clearzoom();
		else if (e.target.id == "search") search_prompt();
		else if (e.target.id == "ignorecase") toggle_ignorecase();
	}, false)

	// mouse-over for info
	// show
	window.addEventListener("mouseover", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = "Function: " + g_to_text(target);
	}, false)

	// clear
	window.addEventListener("mouseout", function(e) {
		var target = find_group(e.target);
		if (target) details.nodeValue = ' ';
	}, false)

	// ctrl-F for search
	// ctrl-I to toggle case-sensitive search
	window.addEventListener("keydown",function (e) {
		if (e.keyCode === 114 || (e.ctrlKey && e.keyCode === 70)) {
			e.preventDefault();
			search_prompt();
		}
		else if (e.ctrlKey && e.keyCode === 73) {
			e.preventDefault();
			toggle_ignorecase();
		}
	}, false)

	// functions
	function get_params() {
		var params = {};
		var paramsarr = window.location.search.substr(1).split('&');
		for (var i = 0; i < paramsarr.length; ++i) {
			var tmp = paramsarr[i].split("=");
			if (!tmp[0] || !tmp[1]) continue;
			params[tmp[0]]  = decodeURIComponent(tmp[1]);
		}
		return params;
	}
	function parse_params(params) {
		var uri = "?";
		for (var key in params) {
			uri += key + '=' + encodeURIComponent(params[key]) + '&';
		}
		if (uri.slice(-1) == "&")
			uri = uri.substring(0, uri.length - 1);
		if (uri == '?')
			uri = window.location.href.split('?')[0];
		return uri;
	}
	function find_child(node, selector) {
		var children = node.querySelectorAll(selector);
		if (children.length) return children[0];
	}
	function find_group(node) {
		var parent = node.parentElement;
		if (!parent) return;
		if (parent.id == "frames") return node;
		return find_group(parent);
	}
	function orig_save(e, attr, val) {
		if (e.attributes["_orig_" + attr] != undefined) return;
		if (e.attributes[attr] == undefined) return;
		if (val == undefined) val = e.attributes[attr].value;
		e.setAttribute("_orig_" + attr, val);
	}
	function orig_load(e, attr) {
		if (e.attributes["_orig_"+attr] == undefined) return;
		e.attributes[attr].value = e.attributes["_orig_" + attr].value;
		e.removeAttribute("_orig_"+attr);
	}
	function g_to_text(e) {
		var text = find_child(e, "title").firstChild.nodeValue;
		return (text)
	}
	function g_to_func(e) {
		var func = g_to_text(e);
		// if there's any manipulation we want to do to the function
		// name before it's searched, do it here before returning.
		return (func);
	}
	function update_text(e) {
		var r = find_child(e, "rect");
		var t = find_child(e, "text");
		var w = parseFloat(r.attributes.width.value) -3;
		var txt = find_child(e, "title").textContent.replace(/\([^(]*\)$/,"");
		t.attributes.x.value = parseFloat(r.attributes.x.value) + 3;

		// Smaller than this size won't fit anything
		if (w < 2 * 12 * 0.59) {
			t.textContent = "";
			return;
		}

		t.textContent = txt;
		var sl = t.getSubStringLength(0, txt.length);
		// check if only whitespace or if we can fit the entire string into width w
		if (/^ *$/.test(txt) || sl < w)
			return;

		// this isn't perfect, but gives a good starting point
		// and avoids calling getSubStringLength too often
		var start = Math.floor((w/sl) * txt.length);
		for (var x = start; x > 0; x = x-2) {
			if (t.getSubStringLength(0, x + 2) <= w) {
				t.textContent = txt.substring(0, x) + "..";
				return;
			}
		}
		t.textContent = "";
	}

	// zoom
	function zoom_reset(e) {
		if (e.attributes != undefined) {
			orig_load(e, "x");
			orig_load(e, "width");
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_reset(c[i]);
		}
	}
	function zoom_child(e, x, ratio) {
		if (e.attributes != undefined) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = (parseFloat(e.attributes.x.value) - x - 10) * ratio + 10;
				if (e.tagName == "text")
					e.attributes.x.value = find_child(e.parentNode, "rect[x]").attributes.x.value + 3;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseFloat(e.attributes.width.value) * ratio;
			}
		}

		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_child(c[i], x - 10, ratio);
		}
	}
	function zoom_parent(e) {
		if (e.attributes) {
			if (e.attributes.x != undefined) {
				orig_save(e, "x");
				e.attributes.x.value = 10;
			}
			if (e.attributes.width != undefined) {
				orig_save(e, "width");
				e.attributes.width.value = parseInt(svg.width.baseVal.value) - (10 * 2);
			}
		}
		if (e.childNodes == undefined) return;
		for (var i = 0, c = e.childNodes; i < c.length; i++) {
			zoom_parent(c[i]);
		}
	}
	function zoom(node) {
		var attr = find_child(node, "rect").attributes;
		var width = parseFloat(attr.width.value);
		var xmin = parseFloat(attr.x.value);
		var xmax = parseFloat(xmin + width);
		var ymin = parseFloat(attr.y.value);
		var ratio = (svg.width.baseVal.value - 2 * 10) / width;

		// XXX: Workaround for JavaScript float issues (fix me)
		var fudge = 0.0001;

		unzoombtn.classList.remove("hide");

		var el = document.getElementById("frames").children;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var a = find_child(e, "rect").attributes;
			var ex = parseFloat(a.x.value);
			var ew = parseFloat(a.width.value);
			var upstack;
			// Is it an ancestor
			if (0 == 0) {
				upstack = parseFloat(a.y.value) > ymin;
			} else {
				upstack = parseFloat(a.y.value) < ymin;
			}
			if (upstack) {
				// Direct ancestor
				if (ex <= xmin && (ex+ew+fudge) >= xmax) {
					e.classList.add("parent");
					zoom_parent(e);
					update_text(e);
				}
				// not in current path
				else
					e.classList.add("hide");
			}
			// Children maybe
			else {
				// no common path
				if (ex < xmin || ex + fudge >= xmax) {
					e.classList.add("hide");
				}
				else {
					zoom_child(e, xmin, ratio);
					update_text(e);
				}
			}
		}
		search();
	}
	function unzoom(dont_update_text) {
		unzoombtn.classList.add("hide");
		var el = document.getElementById("frames").children;
		for(var i = 0; i < el.length; i++) {
			el[i].classList.remove("parent");
			el[i].classList.remove("hide");
			zoom_reset(el[i]);
			if(!dont_update_text) update_text(el[i]);
		}
		search();
	}
	function clearzoom() {
		unzoom();

		// remove zoom state
		var params = get_params();
		if (params.x) delete params.x;
		if (params.y) delete params.y;
		history.replaceState(null, null, parse_params(params));
	}

	// search
	function toggle_ignorecase() {
		ignorecase = !ignorecase;
		if (ignorecase) {
			ignorecaseBtn.classList.add("show");
		} else {
			ignorecaseBtn.classList.remove("show");
		}
		reset_search();
		search();
	}
	function reset_search() {
		var el = document.querySelectorAll("#frames rect");
		for (var i = 0; i < el.length; i++) {
			orig_load(el[i], "fill")
		}
		var params = get_params();
		delete params.s;
		history.replaceState(null, null, parse_params(params));
	}
	function search_prompt() {
		if (!searching) {
			var term = prompt("Enter a search term (regexp " +
			    "allowed, eg: ^ext4_)"
			    + (ignorecase ? ", ignoring case" : "")
			    + "\nPress Ctrl-i to toggle case sensitivity", "");
			if (term != null) search(term);
		} else {
			reset_search();
			searching = 0;
			currentSearchTerm = null;
			searchbtn.classList.remove("show");
			searchbtn.firstChild.nodeValue = "Search"
			matchedtxt.classList.add("hide");
			matchedtxt.firstChild.nodeValue = ""
		}
	}
	function search(term) {
		if (term) currentSearchTerm = term;

		var re = new RegExp(currentSearchTerm, ignorecase ? 'i' : '');
		var el = document.getElementById("frames").children;
		var matches = new Object();
		var maxwidth = 0;
		for (var i = 0; i < el.length; i++) {
			var e = el[i];
			var func = g_to_func(e);
			var rect = find_child(e, "rect");
			if (func == null || rect == null)
				continue;

			// Save max width. Only works as we have a root frame
			var w = parseFloat(rect.attributes.width.value);
			if (w > maxwidth)
				maxwidth = w;

			if (func.match(re)) {
				// highlight
				var x = parseFloat(rect.attributes.x.value);
				orig_save(rect, "fill");
				rect.attributes.fill.value = "rgb(230,0,230)";

				// remember matches
				if (matches[x] == undefined) {
					matches[x] = w;
				} else {
					if (w > matches[x]) {
						// overwrite with parent
						matches[x] = w;
					}
				}
				searching = 1;
			}
		}
		if (!searching)
			return;
		var params = get_params();
		params.s = currentSearchTerm;
		history.replaceState(null, null, parse_params(params));

		searchbtn.classList.add("show");
		searchbtn.firstChild.nodeValue = "Reset Search";

		// calculate percent matched, excluding vertical overlap
		var count = 0;
		var lastx = -1;
		var lastw = 0;
		var keys = Array();
		for (k in matches) {
			if (matches.hasOwnProperty(k))
				keys.push(k);
		}
		// sort the matched frames by their x location
		// ascending, then width descending
		keys.sort(function(a, b){
			return a - b;
		});
		// Step through frames saving only the biggest bottom-up frames
		// thanks to the sort order. This relies on the tree property
		// where children are always smaller than their parents.
		var fudge = 0.0001;	// JavaScript floating point
		for (var k in keys) {
			var x = parseFloat(keys[k]);
			var w = matches[keys[k]];
			if (x >= lastx + lastw - fudge) {
				count += w;
				lastx = x;
				lastw = w;
			}
		}
		// display matched percent
		matchedtxt.classList.remove("hide");
		var pct = 100 * count / maxwidth;
		if (pct != 100) pct = pct.toFixed(1)
		matchedtxt.firstChild.nodeValue = "Matched: " + pct + "%";
	}
]]>
</script>
<rect x="0.0" y="0" width="1200.0" height="326.0" fill="url(#background)"  />
<text id="title" x="600.00" y="24" >Flame Graph</text>
<text id="details" x="10.00" y="309" > </text>
<text id="unzoom" x="10.00" y="24" class="hide">Reset Zoom</text>
<text id="search" x="1090.00" y="24" >Search</text>
<text id="ignorecase" x="1174.00" y="24" >ic</text>
<text id="matched" x="1090.00" y="309" > </text>
<g id="frames">
<g >
<title>irqtime_account_irq (29,195,617 samples, 0.02%)</title><rect x="1189.1" y="197" width="0.3" height="15.0" fill="rgb(232,99,25)" rx="2" ry="2" />
<text  x="1192.14" y="207.5" ></text>
</g>
<g >
<title>tick_sched_handle (177,622,178 samples, 0.15%)</title><rect x="1187.4" y="133" width="1.7" height="15.0" fill="rgb(211,158,21)" rx="2" ry="2" />
<text  x="1190.41" y="143.5" ></text>
</g>
<g >
<title>__irq_exit_rcu (28,681,710 samples, 0.02%)</title><rect x="1187.1" y="197" width="0.3" height="15.0" fill="rgb(227,128,28)" rx="2" ry="2" />
<text  x="1190.13" y="207.5" ></text>
</g>
<g >
<title>__hrtimer_run_queues (177,622,178 samples, 0.15%)</title><rect x="1187.4" y="165" width="1.7" height="15.0" fill="rgb(216,93,27)" rx="2" ry="2" />
<text  x="1190.41" y="175.5" ></text>
</g>
<g >
<title>update_process_times (177,622,178 samples, 0.15%)</title><rect x="1187.4" y="117" width="1.7" height="15.0" fill="rgb(250,200,37)" rx="2" ry="2" />
<text  x="1190.41" y="127.5" ></text>
</g>
<g >
<title>a_star_v1.0 (121,154,757,643 samples, 100.00%)</title><rect x="10.0" y="261" width="1180.0" height="15.0" fill="rgb(253,65,26)" rx="2" ry="2" />
<text  x="13.00" y="271.5" >a_star_v1.0</text>
</g>
<g >
<title>sync_regs (29,608,180 samples, 0.02%)</title><rect x="1186.8" y="213" width="0.3" height="15.0" fill="rgb(235,61,0)" rx="2" ry="2" />
<text  x="1189.85" y="223.5" ></text>
</g>
<g >
<title>sugov_update_single_freq (29,810,675 samples, 0.02%)</title><rect x="1188.9" y="53" width="0.2" height="15.0" fill="rgb(247,42,19)" rx="2" ry="2" />
<text  x="1191.85" y="63.5" ></text>
</g>
<g >
<title>asm_sysvec_reschedule_ipi (29,601,288 samples, 0.02%)</title><rect x="1189.4" y="229" width="0.3" height="15.0" fill="rgb(219,125,7)" rx="2" ry="2" />
<text  x="1192.43" y="239.5" ></text>
</g>
<g >
<title>hrtimer_interrupt (177,622,178 samples, 0.15%)</title><rect x="1187.4" y="181" width="1.7" height="15.0" fill="rgb(227,181,40)" rx="2" ry="2" />
<text  x="1190.41" y="191.5" ></text>
</g>
<g >
<title>exit_to_user_mode_prepare (29,601,288 samples, 0.02%)</title><rect x="1189.4" y="197" width="0.3" height="15.0" fill="rgb(220,66,14)" rx="2" ry="2" />
<text  x="1192.43" y="207.5" ></text>
</g>
<g >
<title>perf_adjust_freq_unthr_context (29,859,585 samples, 0.02%)</title><rect x="1187.7" y="69" width="0.3" height="15.0" fill="rgb(226,126,17)" rx="2" ry="2" />
<text  x="1190.70" y="79.5" ></text>
</g>
<g >
<title>__irqentry_text_end (29,878,925 samples, 0.02%)</title><rect x="1186.0" y="229" width="0.3" height="15.0" fill="rgb(223,122,50)" rx="2" ry="2" />
<text  x="1188.98" y="239.5" ></text>
</g>
<g >
<title>tick_sched_timer (177,622,178 samples, 0.15%)</title><rect x="1187.4" y="149" width="1.7" height="15.0" fill="rgb(239,41,48)" rx="2" ry="2" />
<text  x="1190.41" y="159.5" ></text>
</g>
<g >
<title>sysvec_apic_timer_interrupt (235,499,505 samples, 0.19%)</title><rect x="1187.1" y="213" width="2.3" height="15.0" fill="rgb(221,52,3)" rx="2" ry="2" />
<text  x="1190.13" y="223.5" ></text>
</g>
<g >
<title>malloc (29,191,174 samples, 0.02%)</title><rect x="1189.7" y="245" width="0.3" height="15.0" fill="rgb(231,74,41)" rx="2" ry="2" />
<text  x="1192.72" y="255.5" ></text>
</g>
<g >
<title>update_load_avg (89,083,241 samples, 0.07%)</title><rect x="1188.3" y="69" width="0.8" height="15.0" fill="rgb(231,132,3)" rx="2" ry="2" />
<text  x="1191.28" y="79.5" ></text>
</g>
<g >
<title>irqentry_exit_to_user_mode (29,601,288 samples, 0.02%)</title><rect x="1189.4" y="213" width="0.3" height="15.0" fill="rgb(226,75,37)" rx="2" ry="2" />
<text  x="1192.43" y="223.5" ></text>
</g>
<g >
<title>[libc.so.6] (118,105,159 samples, 0.10%)</title><rect x="10.0" y="229" width="1.2" height="15.0" fill="rgb(206,47,44)" rx="2" ry="2" />
<text  x="13.00" y="239.5" ></text>
</g>
<g >
<title>compute_path_tab (177,401,136 samples, 0.15%)</title><rect x="12.9" y="245" width="1.7" height="15.0" fill="rgb(205,102,18)" rx="2" ry="2" />
<text  x="15.90" y="255.5" ></text>
</g>
<g >
<title>perf_event_task_tick (29,859,585 samples, 0.02%)</title><rect x="1187.7" y="85" width="0.3" height="15.0" fill="rgb(206,191,5)" rx="2" ry="2" />
<text  x="1190.70" y="95.5" ></text>
</g>
<g >
<title>account_user_time (29,468,102 samples, 0.02%)</title><rect x="1187.4" y="101" width="0.3" height="15.0" fill="rgb(251,74,28)" rx="2" ry="2" />
<text  x="1190.41" y="111.5" ></text>
</g>
<g >
<title>scheduler_tick (148,154,076 samples, 0.12%)</title><rect x="1187.7" y="101" width="1.4" height="15.0" fill="rgb(252,56,7)" rx="2" ry="2" />
<text  x="1190.70" y="111.5" ></text>
</g>
<g >
<title>update_cfs_group (29,211,250 samples, 0.02%)</title><rect x="1188.0" y="69" width="0.3" height="15.0" fill="rgb(252,207,40)" rx="2" ry="2" />
<text  x="1190.99" y="79.5" ></text>
</g>
<g >
<title>__cgroup_account_cputime_field (29,468,102 samples, 0.02%)</title><rect x="1187.4" y="85" width="0.3" height="15.0" fill="rgb(253,26,27)" rx="2" ry="2" />
<text  x="1190.41" y="95.5" ></text>
</g>
<g >
<title>all (121,154,757,643 samples, 100%)</title><rect x="10.0" y="277" width="1180.0" height="15.0" fill="rgb(223,212,23)" rx="2" ry="2" />
<text  x="13.00" y="287.5" ></text>
</g>
<g >
<title>task_tick_fair (118,294,491 samples, 0.10%)</title><rect x="1188.0" y="85" width="1.1" height="15.0" fill="rgb(207,203,25)" rx="2" ry="2" />
<text  x="1190.99" y="95.5" ></text>
</g>
<g >
<title>__schedule (29,601,288 samples, 0.02%)</title><rect x="1189.4" y="165" width="0.3" height="15.0" fill="rgb(206,18,13)" rx="2" ry="2" />
<text  x="1192.43" y="175.5" ></text>
</g>
<g >
<title>get_next_freq (29,810,675 samples, 0.02%)</title><rect x="1188.9" y="37" width="0.2" height="15.0" fill="rgb(249,111,27)" rx="2" ry="2" />
<text  x="1191.85" y="47.5" ></text>
</g>
<g >
<title>__do_softirq (28,681,710 samples, 0.02%)</title><rect x="1187.1" y="181" width="0.3" height="15.0" fill="rgb(238,213,46)" rx="2" ry="2" />
<text  x="1190.13" y="191.5" ></text>
</g>
<g >
<title>schedule (29,601,288 samples, 0.02%)</title><rect x="1189.4" y="181" width="0.3" height="15.0" fill="rgb(210,102,2)" rx="2" ry="2" />
<text  x="1192.43" y="191.5" ></text>
</g>
<g >
<title>[unknown] (297,304,967 samples, 0.25%)</title><rect x="10.0" y="245" width="2.9" height="15.0" fill="rgb(253,14,6)" rx="2" ry="2" />
<text  x="13.00" y="255.5" ></text>
</g>
<g >
<title>is_in_list (120,650,860,366 samples, 99.58%)</title><rect x="14.6" y="245" width="1175.1" height="15.0" fill="rgb(235,128,3)" rx="2" ry="2" />
<text  x="17.62" y="255.5" >is_in_list</text>
</g>
<g >
<title>compute_path_tab (179,199,808 samples, 0.15%)</title><rect x="11.2" y="229" width="1.7" height="15.0" fill="rgb(234,68,14)" rx="2" ry="2" />
<text  x="14.15" y="239.5" ></text>
</g>
<g >
<title>sched_clock_cpu (29,195,617 samples, 0.02%)</title><rect x="1189.1" y="181" width="0.3" height="15.0" fill="rgb(221,222,26)" rx="2" ry="2" />
<text  x="1192.14" y="191.5" ></text>
</g>
<g >
<title>__update_load_avg_se (29,430,469 samples, 0.02%)</title><rect x="1188.6" y="53" width="0.3" height="15.0" fill="rgb(243,177,11)" rx="2" ry="2" />
<text  x="1191.57" y="63.5" ></text>
</g>
<g >
<title>__sysvec_apic_timer_interrupt (177,622,178 samples, 0.15%)</title><rect x="1187.4" y="197" width="1.7" height="15.0" fill="rgb(242,118,46)" rx="2" ry="2" />
<text  x="1190.41" y="207.5" ></text>
</g>
<g >
<title>asm_sysvec_apic_timer_interrupt (323,878,371 samples, 0.27%)</title><rect x="1186.3" y="229" width="3.1" height="15.0" fill="rgb(206,60,1)" rx="2" ry="2" />
<text  x="1189.27" y="239.5" ></text>
</g>
<g >
<title>__accumulate_pelt_segments (29,430,469 samples, 0.02%)</title><rect x="1188.6" y="37" width="0.3" height="15.0" fill="rgb(220,222,31)" rx="2" ry="2" />
<text  x="1191.57" y="47.5" ></text>
</g>
</g>
</svg>
